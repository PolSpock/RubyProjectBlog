.row
  .col-xs-2
    %h1{style: 'color: orange;'} Blog
.br
.row
  .col-xs-8
    %h1{style: 'color: orange;'} Les postes :
    - Post.listing.each do |post|
      %h2= post.title
      %h3
        %a{href: posts_path(category_id: post.category_id), class:'category'}
          %span{class:"badge"}=post.category.name
      %p= post.content
      %hr
  .col-xs-4{style: 'background-color: #f9f9f9;'}
    %h1{style: 'color: orange;'} Les catégories :
    - Category.listing.each do |category|
      %h3
        %a{href: posts_path(category_id: category.id), class:'category'}= category.name
.row
  %button{class:'btn btn-primary pull-right'}
    =link_to "Ajouter", admins_path, style: 'color: white'
